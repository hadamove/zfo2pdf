{
  "dependencies": {
    "express": "^4.18.3"
  },
  "name": "zfo",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.2",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.25",
    "esbuild": "0.20.1",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.4.2"
  },
  "scripts": {
    "build:js": "esbuild --bundle src/index.js --outfile=app.js --minify",
    "build:css": "npx tailwindcss -i ./src/style.css -o ./style.css --minify",
    "build": "npm run build:js && npm run build:css",
    "deploy": "npm run build",
    "start": "npm run build && node main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}